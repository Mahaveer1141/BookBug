<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookApp">
        <activity android:name=".ShowFollowingActivity"></activity>
        <activity android:name=".ShowFollowersActivity" />
        <activity android:name=".ShowFollowers" />
        <activity android:name=".ShowCollectonActivity" />
        <activity android:name=".ShowBookActivity" />
        <activity android:name=".ShowUserActivity" />
        <activity android:name=".AddPostActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".SearchUserActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditPostActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>